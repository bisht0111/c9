<% include partials/header_user %>

<div class="ui main text container segment">
  <div class="ui huge header">Edit <%= student.name %> </div>
  <form class="ui form" action="/user/students/<%= student._id %>?_method=PUT" method="POST">
      <div class="field">
        <div class="field">
                  <label>Student ID:</label>
                  <input type="text" value="<%= student.studentid %>" name="student[studentid]">
                </div>
        <label>Reserve Book</label>
          <ol>
            <li>
                <div class="field">
                  <label>Book ID</label>
                  <input type="text" value="<%= student.reserve[0].booksid %>" name="student0[booksid]">
                </div>
                <div class="field">
                  <label>Reserve Date</label>
                  <input type="text" value="<%= student.reserve[0].reservedate %>" name="student0[reservedate]">
                </div>
            </li>
            <li>
                <div class="field">
                  <label>Book ID</label>
                  <input type="text" value="<%= student.reserve[1].booksid %>" name="student1[booksid]">
                </div>
                <div class="field">
                  <label>Reserve Date</label>
                  <input type="text" value="<%= student.reserve[1].reservedate %>" name="student1[reservedate]">
                </div>
            </li>
          </ol>
      </div>
      <input class="ui violet big basic button" type="submit">
  </form>
</div>

<% include partials/footer_user%>